import { Poppins, Rubik_Glitch } from "next/font/google";

export const rubikGlitch = Rubik_Glitch({
	subsets: ["latin"],
	weight: "400",
	display: "swap",
});
export const poppins = Poppins({
	subsets: ["latin"],
	weight: ["400", "700"],
	display: "swap",
});
